import {
    sidebarToggle,
    stickyHeader,
    logoSlider,
    initCarousel
} from './index.js';

document.addEventListener('DOMContentLoaded', () => {
    
    sidebarToggle();
    stickyHeader();
    logoSlider();
    initCarousel();
});
