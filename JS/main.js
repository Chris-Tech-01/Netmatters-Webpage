import {
    sidebarToggle,
    stickyHeader,
    logoSlider
} from './index.js';

document.addEventListener('DOMContentLoaded', () => {
    
    sidebarToggle();
    stickyHeader();
    logoSlider();
});
